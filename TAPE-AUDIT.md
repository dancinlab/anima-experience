# TAPE-AUDIT — anima-experience

Static HF Space (10-tab Gradio showcase + Canvas/JS mutual-information visualizer). Sister of `anima-emergence`. Pure front-end demo — minimal surface.

## A. Audit-class ledgers (cargo / migration candidates)

None. No `state/`, no `.jsonl`, no `*.marker`, no `*audit*/` dir. The repo is `index.html` (54 KB) + `tabs/emergence.py` + READMEs.

## B. Identity surface

None on disk. The "identity" is the HF Space front-matter (`title: Anima Experience`, `emoji: ✨`, `sdk: static`). Not a candidate for `identity.tape` — the project itself has no internal state.

## C. Domain.md files

None. `AGENTS.md` (empty), `CLAUDE.md` (symlink), `README.md` only. No `UPPERCASE.md` per-subject convention.

## D. Per-run / per-event history surfaces

Client-side animation only (`requestAnimationFrame` MI visualization, `H(L) + H(R) − H(L, R)` per tick). No server roundtrip, no recorded history — by design. If anything were ever recorded it would be browser-local sample streams (entropy time-series), but nothing is persisted today.

## E. Promotion candidates

- **n6 atoms** — One genuine atom: `emergence = H(L) + H(R) − H(L, R)` MI formula. Worth promoting once to atlas, no per-repo tape needed.
- **hxc / n12** — N/A (no binary wire, no metric cube).

**Verdict: NONE** (zero tape surfaces; one promotable n6 atom).
